{
  "name": "pawfectcare-security-tests",
  "private": true,
  "scripts": {
    "test:rules": "npx --yes firebase-tools@latest emulators:exec --only firestore,storage \"node --test rules-tests/*.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2"
  }
}
